[package]
name = "colver-py"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "_colver"
crate-type = ["cdylib"]

[dependencies]
colver-core = { path = "../colver-core" }
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"
rand = "0.8"
